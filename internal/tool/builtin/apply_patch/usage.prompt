对已有文件执行 Codex 风格增量编辑。通过 `patch` 参数提交完整补丁文档（`*** Begin Patch` … `*** End Patch`），用 `@@` hunk 与 `-`/`+` 行在文件中定位并修改内容。创建新文件或覆盖整个文件，请优先使用 {{.WriteFile}}；编辑已有文件优先使用本工具。

使用规则：

1. 编辑某个文件前，须在本会话内对该文件成功 {{.ReadFile}}，且须先看到 read 返回内容后再 apply_patch。同一条回复中不能既 {{.ReadFile}} 某文件又 apply_patch 该文件；未读过或 read 与 patch 同批时会直接报错。`*** Add File:` 不要求先读。
2. 基于 {{.ReadFile}} 返回的内容编写 patch 时，必须严格保留行号后缀后方原有的缩进格式（制表符/空格）。行号前缀格式为 `行号|`，其后的所有内容才是需要匹配的真实文件内容。**patch 中的上下文行、`-` 行、`+` 行严禁包含任何 `行号|` 前缀**。
3. 优先对代码库内已有文件进行修改（`*** Update File:`）；除非用户明确要求，否则禁止新建文件（`*** Add File:`）。
4. 仅在用户明确要求时才能使用表情符号，无相关需求时不要在文件中添加表情。
5. 若某个 hunk 的 `-` 行（待删除的旧内容）在定位范围内匹配不唯一，本次编辑会失败。可在 `@@` 后添加上下文行，或扩充 `-`/`+` 块使其具备唯一性。
6. 跨全文批量替换、重命名字符串：在同一文件的 patch 中写多个 `@@` hunk，对每个匹配处分别列出 `-`/`+` 行；变量批量重命名等场景常用此方式。
7. 使用`*** End of File` 标记文件末尾 hunk。
8. 任一 hunk 失败则整次 patch 原子回滚、不写盘。

## Patch 格式概要

```text
*** Begin Patch
*** Update File: path/to/file.go
@@
 可选上下文行
-旧行
+新行
*** Add File: new/file.go
+内容行
*** Delete File: obsolete.go
*** End Patch
```

文件重命名：在 `*** Update File:` 块内、`@@` 之前写 `*** Move to: 新路径`；先应用 hunk 修改内容，再将文件移动到新路径（目标已存在则失败）。示例：

```text
*** Begin Patch
*** Update File: old/path.go
*** Move to: new/path.go
@@
-旧行
+新行
*** End Patch
```
