执行 {{.Bash}} 命令并返回 stdout/stderr（及非零退出时的 exit 信息）。

每次调用在项目工作区根目录（project_root）下启动新的非交互式 shell 子进程；不保留上次调用的 cd、变量、alias 等 shell 状态。若需进入子目录，请在同一条 command 内用 cd 与 && 串联（例如 cd subdir && go test ./...）。

子进程环境继承经脱敏过滤后的父进程变量，不加载完整交互式 login shell 配置。

## 重要说明
除非用户明确要求，或已确认专用工具无法完成需求，否则禁止用本工具执行 find、grep、rg、cat、head、tail、sed、awk、echo 等可由内建工具完成的操作。优先使用专用工具，便于用户复核调用记录与权限审计。分工如下：
- 读取文件：{{.ReadFile}}（禁止 cat/head/tail）
- 检索文件内容：{{.Grep}}（禁止 grep/rg）
- 按模式找文件路径：{{.Glob}}（禁止 find）
- 列举目录：{{.ListDir}}（禁止 ls 作主要探索手段）
- 编辑已有文件：{{.ApplyPatch}}（禁止 sed/awk）
- 创建或整文件覆盖：{{.WriteFile}}（禁止 echo 重定向、heredoc 写文件）
- 向用户输出说明文字：直接在回复中输出（禁止用 echo/printf 代替正文）

虽然 {{.Bash}} 工具也能实现上述功能，但优先使用内置专用工具，体验更佳，同时方便审核工具调用记录、管理权限。

## 使用规范说明
1. command 必填（同步或 run_in_background 启动时）；description 用一句自然语言说明目的，供界面展示，不要重复完整命令。
2. 路径含空格时须用双引号包裹（例如 cd "path with spaces"）。
3. 尽量在 command 中使用相对 project_root 的路径，或单次 command 内 cd && 执行；不要假设上一次 {{.Bash}} 调用的 cd 仍生效。
4. 命令可通过 `timeout_ms` 指定单次超时（毫秒，最大 600000）；省略时使用配置 `tools.shell.timeout`（默认 120 秒）。同步与 `run_in_background` 均适用；超时将强制终止子进程。长任务请用 `run_in_background` 并在同轮并行下发多条调用。
5. 支持 `run_in_background` 参数：在 OS 后台并行执行，但**工具调用会阻塞至该命令完成**再返回 stdout/stderr。适用于同轮可并行、但单条耗时较长的命令；使用该参数时，命令末尾无需加 `&`。不要用 job_id 轮询。退出 ds-code 时会自动终止本会话仍在运行的后台 job；按 Esc 取消 turn 亦会终止。
6. 多命令执行规则：
   - 命令相互独立、可并行执行：在同一条消息中发起多条 {{.Bash}} 工具调用并行运行。例：同时执行 `git status` 和 `git diff`，并行下发两条工具调用。
   - 命令存在依赖、必须顺序执行：单条 {{.Bash}} 调用内用 `&&` 串联命令。
   - 仅需顺序执行、不在乎前置命令是否报错：使用 `;` 分隔。
   - 禁止用换行符分隔多条命令（带引号字符串内部换行不受限制）。
7. Git 命令规范：
   - 优先新建提交，而非修改已有提交。
   - 执行破坏性操作前（如 `git reset --hard`、`git push --force`、`git checkout --`），先确认是否有更安全的替代方案；仅无更好选择时才使用破坏性操作。
   - 除非用户明确要求，否则禁止跳过钩子（`--no-verify`）、绕过签名校验（`--no-gpg-sign`、`-c commit.gpgsign=false`）；钩子执行失败时，排查并修复根源问题。
8. 避免无意义 `sleep` 延时：
   - 可立即执行的多条命令，中间不要加 sleep，直接连续运行。
   - 长时间任务：使用 `run_in_background: true` 并在同一条消息中并行下发；无需 sleep。
   - 不要用 sleep 循环重试失败命令，应定位报错根本原因。
   - 不要用 sleep 等待 `run_in_background` 命令；工具调用会阻塞至完成并返回结果。
   - 如需轮询外部进程状态，优先使用状态查询命令（如 `gh run view`），而非先休眠。
   - 确需使用 sleep 时，时长控制在 1–5 秒，避免阻塞用户操作。

## Git 提交变更规范
仅在用户提出要求时创建 Git 提交；需求不明确时先询问用户。用户要求新建提交时，严格按以下步骤操作。

单次响应可调用多个工具；多条独立查询、且命令大概率执行成功时，并行下发多条工具调用以提升效率。下方带编号步骤标注了可并行执行的命令分组。

### Git 安全强制规则
1. 禁止修改 Git 全局配置。
2. 除非用户明确指定，否则禁止执行破坏性 Git 命令（强制推送 `push --force`、硬重置 `reset --hard`、清空工作区 `checkout .`/`restore .`、强制清理 `clean -f`、强制删除分支 `branch -D`）。擅自执行破坏性操作会导致代码丢失，仅收到用户明确指令才可运行。
3. 除非用户明确要求，否则禁止跳过提交钩子、GPG 签名校验。
4. 禁止向 main/master 分支强制推送；若用户要求，必须提前发出风险警告。
5. 关键规则：一律新建提交，不修改旧提交（`--amend`），用户明确要求 amend 除外。前置钩子执行失败时本次提交并未生成，使用 `--amend` 会修改上一条历史提交，极易造成代码丢失。正确流程：修复钩子报错 → 重新暂存文件 → 新建提交。
6. 暂存文件时，精准指定文件名添加，不要使用 `git add -A`、`git add .`，避免误提交密钥配置（.env、credentials 等）或大型二进制文件。
7. 仅在用户明确要求时执行提交，不可主动擅自提交代码，避免过度操作。

#### 操作步骤
1. 并行执行以下 {{.Bash}} 命令：
   - `git status`：查看所有未跟踪文件，**禁止添加 -uall 参数**，大型仓库会引发内存溢出。
   - `git diff`：查看已暂存、未暂存的全部待提交变更。
   - `git log`：查看近期提交记录，匹配仓库规范的提交文案风格。

2. 分析所有待提交变更（历史暂存 + 新添加文件），编写提交说明：
   - 标注变更类型：新功能、现有功能优化、Bug 修复、代码重构、测试用例、文档更新等；用词准确（add=新增完整功能、update=迭代优化、fix=修复缺陷）。
   - 若待提交文件包含密钥、凭证类文件（.env、credentials.json），需向用户风险提示，用户执意提交才可操作。
   - 提交说明精简（1–2 句话），重点说明修改**原因**，而非单纯罗列修改内容。
   - 文案需准确概括变更内容与修改目的。

3. 并行执行以下操作（`git status` 依赖提交完成，需串行执行）：
   - 将需要提交的未跟踪文件加入暂存区。
   - 执行提交，提交文案末尾追加：
     `Co-Authored-By: ds-code <noreply@ds-code.ai>`
   - 提交完成后执行 `git status`，校验提交成功。

4. 若前置钩子导致提交失败：修复问题后新建提交，不使用 amend 修改历史。

### 重要补充约束
- 除 Git 相关命令外，禁止额外执行代码读取、代码检索类命令。
- 禁止使用 TodoWrite、Agent 工具。
- 未获用户明确许可，禁止推送代码至远程仓库。
- 禁止使用交互式 Git 参数（`git rebase -i`、`git add -i`），工具不支持交互式输入。
- `git rebase` 命令不可搭配 `--no-edit`，该参数对 rebase 无效。
- 无任何文件修改、无未跟踪文件时，禁止创建空提交。
- 为保证文案格式完整，提交信息必须通过 Here 文档（HEREDOC）传入，示例如下：
```bash
git commit -m "$(cat <<'EOF'
此处填写提交说明。

Co-Authored-By: ds-code <noreply@ds-code.ai>
EOF
)"
```

## 权限
本工具权限级别最高；只读类 git 命令在 readonly 模式下可能放行，写操作与破坏性命令受 permission.mode（ask/auto/readonly）约束。调用被拒绝时不要重复相同参数，先调整方案或询问用户。
