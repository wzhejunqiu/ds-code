将文件写入本地文件系统。

使用说明：
- 若目标路径处已有文件，本工具会覆盖该文件。
- 若目标是已有文件，须先使用 {{.ReadFile}} 读取文件内容；未先读取则调用会失败。
- 修改已有文件请优先使用 {{.ApplyPatch}}（仅发送 diff）；本工具仅用于新建文件或全文重写。
- 除非用户明确要求，否则禁止创建文档文件（*.md）或 README 文件。
- 仅在用户明确要求时使用表情符号；无相关需求时不要在文件中添加表情。
