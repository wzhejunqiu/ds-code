基于 ripgrep 的文件路径 glob 搜索工具。

- 快速的文件路径 glob 匹配工具，适用于任意规模的代码库
- 按文件名模式查找路径**必须**调用 {{.Glob}} 工具，**禁止**通过 {{.Bash}} 执行 `find`；本工具已针对权限与工作区访问优化
- 支持 glob 模式，例如 `**/*.go`、`src/**/*.go`；用 `path` 收窄搜索根目录（必须是目录，默认 `.`）
- 返回匹配的文件路径，按修改时间降序排序；路径一律**相对项目根**（正斜杠），可直接传给 {{.ReadFile}} / {{.Grep}}，不是相对 `path` 搜索目录
- 默认不遵循 `.gitignore`，并包含隐藏文件
- 不搜索 `.git`（含 `path=.git` 返回无匹配）；宽泛 `path=.` 时可能跳过部分目录，显式收窄 `path` 可进入
- 输出格式：`Found N files` + 路径列表；结果过多时末尾会出现截断提示，应收窄 `pattern`/`path`
- 需要按文件名模式查找文件时使用本工具
- 开放、需多轮 glob 与 grep 探索的搜索任务，请改用 {{.Agent}} 工具
