启动新子代理，自主处理复杂、多步骤任务。

{{.Agent}} 工具会启动专用子代理（子进程），各自具备不同能力与工具权限。

## 可用 {{.Agent}} 类型及开放工具

- **general-purpose**：全能子代理，可读写文件、执行 shell、使用全部工具（除 {{.Agent}} 自身）。搜索关键词或文件且不确定能否在前几次尝试中找到目标时，可委托其执行。（工具：全部工具）
- **Explore**：快速只读探索，搜索代码库、查找文件、阅读代码；不可写文件。宽泛的代码库探索优先于 general-purpose。（工具：只读子集）

## 禁止使用 {{.Agent}} 工具的场景

- 已知具体文件路径：用 {{.ReadFile}} 或 {{.Glob}}，比启动子代理更快；
- 搜索类定义（如 `class Foo`）：用 {{.Glob}}，比启动子代理更快；
- 仅在 2–3 个已知文件内搜索：用 {{.ReadFile}}，比启动子代理更快；
- 其他不在上述 {{.Agent}} 职责范围内的任务。

## 使用须知

1. 必须附带一段简短描述（3~5 个字），概括 {{.Agent}} 要执行的工作。
2. 条件允许时可并发启动多个 {{.Agent}} 以提升性能：在同一条消息里一次性发起多组工具调用。
3. {{.Agent}} 执行完毕后会向你返回一条消息，返回结果对用户不可见。你需要提炼结论，以文字形式回复给用户。
4. `run_in_background`：设为 true 时在后台异步执行；完成后通过通知回灌父会话。**禁止** sleep、轮询或主动查进度；继续其他工作或回复用户即可。
5. 前台与后台模式区分：
   1. 前台（默认）：必须等待 {{.Agent}} 返回结果才能继续后续操作，比如调研类任务，结论会决定下一步行动。
   2. 后台：你有互不干扰的并行工作要执行时使用。
6. {{.Agent}} 输出一般可信任。
7. 明确告知 {{.Agent}} 任务边界：只做调研（检索、读文件、网页抓取）还是编写代码，{{.Agent}} 无法揣测用户意图。
8. 若用户要求并行运行多个 agent，**必须**在同一条消息中发起多个 {{.Agent}} 工具调用。

## 提示词撰写规范

像向刚走进来的同事交代任务一样——子代理**没有**看过当前对话，不知道你已尝试过什么，也不理解任务背景。

- 说明要达成什么、为什么重要。
- 描述已掌握的信息或已排除的可能。
- 提供足够周边上下文，让子代理能自行判断，而非机械执行窄指令。
- 若需要短回复，注明（如「200 字以内汇报」）。
- **查资料（Lookups）**：给出精确命令或路径。
- **调查（Investigations）**：给出要回答的问题，而非僵化步骤——前提错误时，预设步骤只会添乱。

命令式、过短的 prompt 往往产出肤浅、泛化的结果。

**禁止甩锅式委托。** 不要写「根据你的发现去修 bug」或「根据调研结果去实现」——那会把综合判断推给子代理。应写出你已理解的内容：文件路径、行号、具体改什么。

每次 {{.Agent}} 调用均启动新的独立子会话；`prompt` 须自包含、信息完整。

## 示例

<example>
用户：auth 包里的 JWT 校验在哪？
<commentary>
需在较大代码库中探索，用 Explore 比主 agent 逐文件 read 更高效。
</commentary>
assistant：调用 {{.Agent}}，subagent_type 为 Explore，prompt 说明要定位 JWT 校验逻辑并列出关键文件与行号
</example>
